let User = ~Struct["User" :first_name :last_name :age]

impl User = {
  fn name() = str(this.first_name " : " this.last_name)
}

impl Validation for User = {
  age: _::len()::pipe(0..10)
}

new User("marcelle" "rusu", "_" * 10).name()::log()